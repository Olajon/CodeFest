
function checkLcm (a, b) {
  var l = Math.max (a,b)
  var s = Math.min (a,b)
  var result = l
      function checkModule (x, y){
      if (x % y === 0) {
      return true
    }

    else {

      return false

        }
      }
      while (!checkModule (l,s)) {
        l += result


          }
          return l
}
